# workerd and esbuild ship prebuilt binaries via postinstall; wrangler's local
# runtime and the vite build both need them.
onlyBuiltDependencies[]=esbuild
onlyBuiltDependencies[]=workerd
onlyBuiltDependencies[]=sharp
